

## Plan: Add LLMs.txt File

Add the provided content as `public/llms.txt` so it's served at the site root (`/llms.txt`).

### File to Create

| File | Action |
|---|---|
| `public/llms.txt` | Create with the provided content |

No other changes needed — Vite serves files from `public/` at the root automatically.

